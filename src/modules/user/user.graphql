type Query {
  me: User
}

type Mutation {
  register(
    name: String
    email: String!
    password: String!
    mobile: String
  ): User
  login(email: String!, password: String!): User
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  mobile: String!
}
