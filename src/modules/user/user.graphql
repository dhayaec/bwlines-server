type Query {
  me: UserResponse
  getUser(id: ID!): UserResponse
}

type Mutation {
  register(
    name: String!
    email: String!
    password: String!
    mobile: String
  ): UserResponse
  login(email: String!, password: String!): UserResponse
  reset(email: String!): UserResponse
  logout: String
}

type UserResponse {
  id: ID!
  name: String!
  email: String!
  mobile: String
}

type User {
  id: ID!
  name: String!
  email: String!
  password: String!
  mobile: String
}
